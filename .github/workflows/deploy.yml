name: Deploy to GitHub Pages

on:
  push:
    branches:
      - main  # Cambia esto si usas otro branch principal

permissions:
  contents: read
  pages: write
  id-token: write

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    steps:
      - name: 🚀 Checkout repo
        uses: actions/checkout@v4

      - name: 📦 Install dependencies
        run: npm install

      - name: 🔧 Build project
        run: npm run build

      - name: 📤 Deploy to GitHub Pages
        uses: JamesIves/github-pages-deploy-action@v4
        with:
          branch: gh-pages  # Rama donde se publicará el sitio
          folder: dist       # Carpeta donde Vite genera el build
